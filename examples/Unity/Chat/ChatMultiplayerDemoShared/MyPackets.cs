namespace ChatMultiplayerDemoShared
{
    /// <summary>
    /// This enum is responsible for represents the packets of messages to client and server handlers.
    /// </summary>
    public enum MyPackets : byte
    {
        Message
    }
}
